# Results

Each result directory corresponds to one committed full run under `runs/`.
Derived scores are reproducible from the raw response evidence and the frozen
dataset, prompt, parser, and scoring rules. Release-level comparisons live in
`releases/`.

Smoke runs and unsuccessful provider attempts are collection guardrails, not
published measurements, and are intentionally absent from this public record.
